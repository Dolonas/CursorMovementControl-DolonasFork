<Window x:Class="WorkEmulation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:commands="clr-namespace:WorkEmulation.Infrastructure.Commands"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        xmlns:local="clr-namespace:WorkEmulation"
        mc:Ignorable="d"
        Title="Имитация движения курсора" Height="190" Width="416">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15" />
            <RowDefinition Height="84" />
            <RowDefinition Height="15" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.05*" />
            <ColumnDefinition Width="2.6*" />
            <ColumnDefinition Width="2.6*" />
            <ColumnDefinition Width="0.05*" />
        </Grid.ColumnDefinitions>
        
        <DockPanel Grid.Row="1" Grid.Column="1" >
            <TextBox x:Name="x1_text" DockPanel.Dock="Top" Height="23"  TextWrapping="Wrap" Text="600" VerticalAlignment="Top" Width="120"/>
            <TextBox x:Name="y1_text" DockPanel.Dock="Top"  Height="23"  TextWrapping="Wrap" Text="300" VerticalAlignment="Top" Width="120"/>
            <TextBox x:Name="x2_text" DockPanel.Dock="Top"  Height="23" TextWrapping="Wrap" Text="300" VerticalAlignment="Top" Width="120"/>
            <TextBox x:Name="y2_text" DockPanel.Dock="Top" Height="23" TextWrapping="Wrap" Text="600" VerticalAlignment="Top" Width="120"/>
        </DockPanel>
        <StackPanel Grid.Row="1" Grid.Column="2"  Orientation="Vertical">
            <Button
                Margin="5 0 0 0"
                HorizontalAlignment="Left"
                Command="{Binding move_cursor_imitate_behaviour}"
                Content="Move cursor smoothly"
                IsEnabled="{Binding IsButtonOfActiveDocumentEnabled}"
                Style="{StaticResource MaterialDesignPaperButton}"
                ToolTip="Прочитать активную открытую сборку в Компасе" />
        
            <Button
                Margin="5 0 0 0"
                HorizontalAlignment="Left"
                Command="{Binding move_cursor_imitate_behaviour}"
                Content="Move cursor directly"
                IsEnabled="{Binding IsButtonOfActiveDocumentEnabled}"
                Style="{StaticResource MaterialDesignPaperButton}"
                ToolTip="Прочитать активную открытую сборку в Компасе" />
        </StackPanel>
        
    </Grid>
</Window>